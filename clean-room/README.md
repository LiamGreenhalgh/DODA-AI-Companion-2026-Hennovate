# Clean-room record

This directory is the source-controlled implementation and asset ledger. `pnpm cleanroom:validate` enforces one record per completed registry behavior, one implementation basis, explicit inaccessible dependencies and observable differences for approximations, evidence existence, and exclusion of assets without a documented permission basis.
